name: histology_nf
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch>=2.0.0
  - torchvision>=0.15.0
  - cudatoolkit=11.8  # Adjust based on your CUDA version
  - pip:
    - transformers==4.42.4
    - tokenizers>=0.19.0
    - environs==11.0.0
    - sacremoses==0.1.1
    - opencv-python>=4.8.0
    - Pillow>=10.0.0
    - scikit-image>=0.21.0
    - numpy>=1.24.0
    - scipy>=1.11.0
    - pandas>=2.0.0
    - openslide-python>=1.3.0
    - cucim>=23.8.0
    - einops>=0.7.0
    - tqdm>=4.66.0
    - pyyaml>=6.0
    # TRIDENT should be installed separately:
    # pip install -e /path/to/TRIDENT
    # CHIEF should be installed separately:
    # Follow CHIEF installation instructions

